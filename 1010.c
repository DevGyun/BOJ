#include <stdio.h>
int countCase(int siteL, int siteR)
{
    if(siteL == 1) return siteR;
    int totalCase = 0;
}
int main()
{
    int i, t, n, m, cases;
    scanf("%d", &t);

    for(i = 0; i < t; i++)
    {
        scanf("%d%d", &n, &m);
    }
}